---
description: barem-site homepage screenshot format (raw UI + real iPhone frame PNG)
globs: public/screenshots/**,public/brand/iphone-16-pro-frame-v2.png,src/components/phone-shot.tsx,src/app/page.tsx,docs/screenshots/**,docs/brand/**
alwaysApply: false
---

# Website screenshots (canonical format)

When updating homepage / marketing-site screenshots for new features, **match the currently live format**. Do not invent a new framing style.

## Canonical asset

- **Path:** `public/screenshots/*.png` (deployed copy: `docs/screenshots/`)
- **Content:** App UI only — status bar + Dynamic Island included in the capture
- **Not in the PNG:** device chassis/bezel, store headlines, disclaimers, cream marketing canvas
- **Size:** **414 × 900** PNG (RGBA), ~ same aspect as iPhone raw `1320×2868`
- **Background:** app cream (`#F7F6F3`), full-bleed screen capture

## How the phone look is made

`PhoneShot` composites each PNG into **`public/brand/iphone-16-pro-frame-v2.png`** (iPhone 16 Pro Black Titanium with **hardware Dynamic Island kept**). Capture islands are **erased from screenshot PNGs** (cream fill); status bar time/icons remain. Screen placement from `iphone-16-pro-template.json` (`x=102 y=100 w=1206 h=2622` in `1406×2822`). Bump the frame filename (`-v2`, `-v3`, …) when replacing the asset so browsers don’t keep a cached island. **Do not** add CSS pill borders. **Do not** bake the bezel into the screenshot PNG.

Source note: frame from [device-frames-media](https://github.com/jonnyjackson26/device-frames-media) (Apple iPhone 16 Pro / Black Titanium).

## Source → site

1. Capture / refresh `barem/store_assets/screenshots/raw/ios/` (full-res)
2. Export site size into `public/screenshots/` (same filenames `01_home.png` … `07_profile.png`):

```bash
# from barem-site, or see README 「스크린샷」
../barem/tool/export_site_screenshots.sh
```

3. `npm run deploy` → commit `public/screenshots` + `docs` (+ frame PNG if changed)

## Rejected formats (do not use on the site)

- Store marketing composites (`barem/store_assets/screenshots/ios/` with headlines)
- Baking a bezel into the site PNG **and** also drawing CSS/`PhoneShot` chassis (double frame)
- Hand-drawn “looks like phone” SVG with mismatched screen radius
- Dropping raw 1320×2868 without resizing (oversized; break `PhoneShot` width/height contract)
- Scraped Apple marketing composites used as the screenshot itself
